TO DO:

1.General Cluster info:

 1.1.Cluster name: Total number of nodes(cluster_info.json),  OS version(if supported, cluster_info.jso), aws_instance_type(determine storage type,cluster_info.jso)
  -> DC name(Workloadtype, number of tokens): number of nodes, rack_map(cluster_info.jso)
  -> each node(node_info.json):
      - ip address, hostname, dse version(cassandra version), "user.timezone" : "America/New_York"(java_system_properties.json),"java.version" : "1.8.0_77" (java_system_properties.json),workload type, ntp status(ntptime),machine memory(machine-info.json),num_procs, used cpu(cpu.json)

ntp_gettime() returns code 5 (ERROR)

ntpstat
exit status: 1

stdout:
unsynchronised
  time server re-starting
   polling server every 8 s


ntp_gettime() returns code 0 (OK)

synchronised to NTP server

2.status

Each node:
3.info: heap memory, uptime

4.tpstats: dropped messages, blocked messages

5.netstats: read repair attempted & blocked

6.cfstats: large partitions(> 256mb) for each table, tombstones(> 0) for each table, Maximum live cells per slice for each table

7.describecluster: for each node Schema versions: UNREACHABLE nodes

8.system.log:
  (categirzed by date)
GC latency , local pauses, hinted handoff,discarded prepared statements(top 10),large batches(top10),dropped messages, tombstone warnings(top10), large partitions(top 10)




